# TEMPLATE ONLY — DO NOT COMMIT REAL SECRETS.
#
# Copy this file to a private location (e.g. secret.yaml, which is gitignored
# or kept out of the repo entirely), replace every REPLACE_ME with the real
# value, and apply it out-of-band:
#
#   cp k8s/secret.example.yaml /tmp/sentinel-secret.yaml
#   $EDITOR /tmp/sentinel-secret.yaml      # fill in real values
#   kubectl apply -f /tmp/sentinel-secret.yaml
#
# Or manage it via a sealed-secrets / external-secrets operator instead.
# Optional keys you do not use may be deleted rather than left as REPLACE_ME.
apiVersion: v1
kind: Secret
metadata:
  name: sentinel-secrets
  labels:
    app: sentinel
type: Opaque
stringData:
  # --- Mandatory ---
  SLACK_BOT_TOKEN: "REPLACE_ME"
  SLACK_APP_TOKEN: "REPLACE_ME"
  BOT_USER_ID: "REPLACE_ME"
  ALLOWED_USER_IDS: "REPLACE_ME"
  # --- Optional ---
  ANTHROPIC_API_KEY: "REPLACE_ME"
  METABASE_URL: "REPLACE_ME"
  METABASE_USERNAME: "REPLACE_ME"
  METABASE_PASSWORD: "REPLACE_ME"
  GITHUB_TOKEN: "REPLACE_ME"
  NOTION_API_KEY: "REPLACE_ME"
  SLACK_USER_TOKEN: "REPLACE_ME"
  GOOGLE_CLIENT_ID: "REPLACE_ME"
  GOOGLE_CLIENT_SECRET: "REPLACE_ME"
  GOOGLE_REFRESH_TOKEN: "REPLACE_ME"
