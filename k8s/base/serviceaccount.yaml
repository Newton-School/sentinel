apiVersion: v1
kind: ServiceAccount
metadata:
  name: sentinel
  labels:
    app: sentinel
# No RBAC bound — Sentinel talks to external APIs (Slack/OpenAI/Metabase/Google),
# not the K8s API. A dedicated SA (vs `default`) keeps it isolated and is the
# attach point for ECR pull / IRSA if you use it.
